/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // images: {
    //   domains: ["bigbasket.com"],
    // },
    unoptimized:true,
  },
};

module.exports = nextConfig
